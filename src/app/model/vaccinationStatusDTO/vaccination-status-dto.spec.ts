import { VaccinationStatusDTO } from './vaccination-status-dto';

describe('VaccinationStatusDTO', () => {
  it('should create an instance', () => {
    expect(new VaccinationStatusDTO()).toBeTruthy();
  });
});

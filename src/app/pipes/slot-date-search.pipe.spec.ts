import { SlotDateSearchPipe } from './slot-date-search.pipe';

describe('SlotDateSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SlotDateSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
